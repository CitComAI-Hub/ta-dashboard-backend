apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-backend
  template:
    metadata:
      labels:
        app: auth-backend
    spec:
      containers:
      - name: auth-backend
        image: ghcr.io/citcomai-hub/ta-dashboard-backend:latest
        ports:
        - containerPort: 4000
        envFrom:
        - secretRef:
            name: auth-backend-secret
        env:
        - name: TIR_BASE_URL
          value: "http://trusted-issuer-list:8080"
        - name: ALLOWED_ORIGINS
          value: "https://auth.54.76.44.128.nip.io"
---
apiVersion: v1
kind: Service
metadata:
  name: auth-backend
spec:
  selector:
    app: auth-backend
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: auth-backend
spec:
  rules:
    - host: auth.54.76.44.128.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: auth-backend
                port:
                  number: 4000 